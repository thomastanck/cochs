define equality =
(\type:*.(\obj1:type.(\obj2:type.
{\z:type->*.((z obj1)->(z obj2))}
)));

define reflexivityofequality =
{\A:*.
{\a:A.

(equality A a a)

}
};

define reflexivity =
(\A:*.
(\a:A.

// we want to output something of type
//   (equality A a a)
// which is secretly
//   {\z:A->*.((z a)->(z a))}
(\z:A->*.
(\p:(z a).p)
)

)
);

(\p:reflexivityofequality.p) reflexivity
